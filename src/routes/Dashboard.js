import React from 'react/addons';
import { RouteHandler } from 'react-router';

export default React.createClass({

  render() {
    return (
      <div>
        <h2>Dashboard</h2>
        <p>Welcome, welcome!</p>
      </div>
    );
  }

});

